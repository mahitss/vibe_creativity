# Web Features

Feature routes own their UI composition, client state, and application services.

Domain behavior should stay behind package or server boundaries. The web app composes experiences; it should not become the source of autonomous agent truth.

